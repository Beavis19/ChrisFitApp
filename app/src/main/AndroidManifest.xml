<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chrisfitapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/solar_eclipse"
        android:label="@string/app_name"
        android:roundIcon="@drawable/solar_eclipse"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChrisFitApp">

        <activity
            android:name=".ui.login.ListActivity"
            android:exported="true">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScrollingActivity"
            android:exported="false"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/Theme.ChrisFitApp.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">



        </activity>
    </application>

</manifest>